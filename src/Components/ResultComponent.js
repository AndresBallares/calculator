
const ResultComponent = (props) => {
    
        return (
            <div className="display">
                <p>{props.result}</p>
            </div>
        );
    
}

export default ResultComponent;
